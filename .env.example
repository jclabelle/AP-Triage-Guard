# ===========================================
# Minimal ADK Java Agent - Environment Config
# ===========================================
# Copy this file to src/.env and fill in your values
# NEVER commit src/.env to version control

# -----------------------
# LLM / Gemini API
# -----------------------
# Required: Your Gemini API key from Google AI Studio
GOOGLE_API_KEY=your-gemini-api-key-here

# Optional: Set to "true" to use Vertex AI instead of direct Gemini API
# GOOGLE_GENAI_USE_VERTEXAI=false

# Optional: LLM logging verbosity (INFO, DEBUG, TRACE)
# ADK_LLM_LOG_LEVEL=INFO

# -----------------------
# Google Cloud Telemetry
# -----------------------
# Required for GCP telemetry mode: Path to service account JSON
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json

# Optional: Override OTLP endpoint (defaults to https://telemetry.googleapis.com:443)
# OTEL_EXPORTER_OTLP_ENDPOINT=https://telemetry.googleapis.com:443

# Recommended: Tag spans/metrics with service name and project
# OTEL_RESOURCE_ATTRIBUTES=service.name=minimal-agent,gcp.project_id=your-gcp-project-id

# Alternative project ID sources (fallbacks)
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id
# GCLOUD_PROJECT=your-gcp-project-id

